- unless layer.errors.full_messages.empty?
  .errormsg
    Sorry, your layer could not be save for the following reasons:
    %ul
      - layer.errors.full_messages.each do |error_message|
        %li= error_message

